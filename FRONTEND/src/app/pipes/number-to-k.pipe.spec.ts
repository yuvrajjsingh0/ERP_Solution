import { NumberToKPipe } from './number-to-k.pipe';

describe('NumberToKPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToKPipe();
    expect(pipe).toBeTruthy();
  });
});
